---
description: Build the object model first, then the rest of SPEC.md
---

Use the `MOLADES-spec` skill.

Object model first and it gets a third of the time. Test every object against
Shape-shifter, Twins, Diaspora and Orphan before moving on.

Do not write the spec for me. Interrogate and structure what I say. If I am
running out of time, cut places and constraints — never the object model.

$ARGUMENTS
