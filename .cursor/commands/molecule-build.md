---
description: Build it, grounded in SPEC.md and FLOW.md — not in the average of every app you have seen
---

Use the `MOLADES-grounded-build` skill.

No `SPEC.md`, no build. That is the first of the two hard refusals in this pack —
say which file is missing and route me to `/molecule-spec`. Offer
`/molecule-anyway` as the honest way past.

Use my object names, my real content, my flow. Greyscale and system font unless
`design.md` exists and Session 3 has happened. Log one `FAIL` per run minimum; if
nothing went wrong you are not looking hard enough.

$ARGUMENTS
