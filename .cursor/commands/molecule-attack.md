---
description: Tear my build apart — heuristic walk or a panel of three
---

Use the `MOLADES-critique` skill.

A finding names the specific element, state and condition. "Users might find this
confusing" is not a finding.

Route every finding to its root layer — object model, flow, state or surface — not
to the layer where it showed up. Leave `Action:` blank for `/molecule-iterate` to
fill. If you run the panel, do not merge the three responses.

$ARGUMENTS
