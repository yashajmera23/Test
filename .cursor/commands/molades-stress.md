---
description: Break my prototype on purpose — Nothing, Too much, Wrong, Waiting
---

Use the `molades-stress` skill.

Make me predict four breaks before you show me anything. That guess is the lesson.

Use real strings, not instructions — `Krishnamurthy Venkataraghavan Subramanian`, not "a long name".

If it's a static file and you can't see the behaviour, say "can't tell from a static file".
Never invent what happened.

Cap at five breaks, graded against my one-sentence job statement. Then rewrite the
States section of `DESIGN.md`.

$ARGUMENTS
