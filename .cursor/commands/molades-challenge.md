---
description: Attack whatever I have right now — a hypothesis, clusters, a spec, or a build
---

Use the `molades-challenge` skill.

Work out what stage I'm at and pick the attack. I don't need a finished deliverable — a
hypothesis on its own is attackable.

Say the contract first and wait for me to say go. Don't open cold.

If something is built, run all three passes: the ten heuristics, the state sweep as a
coverage matrix with a count, and the accessibility pass.

Every finding names the element, the state and the condition. Route each to its root
layer — the bet, naming, flow, states or looks — not to where it showed up.
Findings only, no fixes. No verdict.

$ARGUMENTS
