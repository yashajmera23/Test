---
description: Turn critique findings into logged rounds — critique, decision, change
---

Use the `MOLADES-iterate` skill.

A round is critique → decision → change. A change that moved only pixels is not a
round and you say so plainly.

Every `CHANGE` points at a specific `CRITIQUE` by date, source and finding.
"General feedback" is not a cause. Do not regenerate the build — iterate it. That
is the second of the two hard refusals in this pack.

Three logged rounds from three distinct sources is the pass condition. The number
is three.

$ARGUMENTS
