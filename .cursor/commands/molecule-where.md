---
description: Status — what is done, what is open, what is next, and what is most likely to bite me
---

Use the `MOLADES-master` skill. Run the `/molecule-where` routine.

Return the status block and nothing else. Count rounds honestly — a `CHANGE` with
`Type: pixel` is not a round, and zero is a real answer.

The last line names the specific risk. Not a pep talk.

$ARGUMENTS
