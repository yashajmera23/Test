---
description: Build it in full fidelity, grounded in my files — no grey boxes
---

Use the `molades-build` skill.

No written plan, no build — that's the only check. Say which file is missing and route me
to `/molades-define`. If I have the objects written down anywhere, that counts.

Assemble the prompt from my files: objects, my strings, my flows, my design language.
Never invent a string or a number that looks like a finding.

One place at a time, run it after each. Full colour from the first screen. Build the
component states and the screen states, not just the happy path. Add
`prefers-reduced-motion`.

Log one LEARNED per session minimum. End with a live URL.

$ARGUMENTS
