---
description: Turn the spec into places, steps and a state coverage matrix in FLOW.md
---

Use the `MOLADES-flow` skill.

Places and steps before screens. For every state, name the exit and name the undo.
One-way doors get called out by name.

Produce `FLOW.md` with a `## State coverage` section. Do not send me to
`/molecule-language` — surface is not your job.

$ARGUMENTS
