---
description: Work out what I'm actually building — screens, the main path, and what happens when it breaks
---

Use the `molades-define` skill.

Five things: what screens there are, what each one is for, how someone gets from one
to the next, what they see when it's empty or breaks, and what I'm not building.

Start by agreeing the words — use whatever my participants actually said, not
whatever sounds neater.

No wireframes, no ASCII boxes, no "a card at the top". Text only — and say why once
so it doesn't read as unhelpfulness. The real screens get built in colour two steps
from here.

**No academic vocabulary.** No entities, attributes, relationships, models or
schemas. Say what's on the screen.

Produce `DESIGN.md`. If time is short, cut the other routes — never the main path.

$ARGUMENTS
