---
description: Turn my research into a grounded PRODUCT_CONTEXT.md with every claim tagged
---

Use the `MOLADES-context-build` skill.

Every claim in `PRODUCT_CONTEXT.md` carries `observed`, `inferred` or `assumed`,
and you apply the downgrade rule silently. If I cannot name the artefact behind an
`observed` claim in one sentence, downgrade it.

Do not invent a persona, a quote, a user need or a number I did not give you. If a
required field has no basis in my material, hand me the questions instead.

$ARGUMENTS
